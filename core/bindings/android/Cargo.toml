[package]
name = "alice-core-android"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
alice-core = { path = "../.." }
jni = { version = "0.21", features = ["invocation"] }
android_logger = "0.14"
log = "0.4"
parking_lot = "0.12"
